- name: play-1
  hosts: localhost
  var_files:
  - variables.yaml
  tasks:
    - name: sub-task
      vars:
       course: "devops with ansible"
      ansible.builtin.debug:
        msg: "HI this is a sample program of {{ course}} about {{trainer}}"
